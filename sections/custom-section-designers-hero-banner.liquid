{{ 'custom-section-designers-hero-banner.css' | asset_url | stylesheet_tag }}

<div class="designers-hero-banner">
  <div class="page-width">
    <div class="designers-banner-wrapper">
      {% if section.settings.desktop_image != blank %}
        <div class="image">
          <img src="{{ section.settings.desktop_image | image_url }}" height="auto" width="auto" class="desktop-img small-hide"> 
          {% if section.settings.mobile_image != blank %}
            <img src="{{ section.settings.mobile_image | image_url }}" height="auto" width="auto" class="mobile-img large-up-hide medium-large-hide medium-hide"> 
          {% else %}
            <img src="{{section.settings.desktop_image | image_url }}" height="auto" width="auto" class="desktop-img">
          {% endif %}
        </div>
      {% endif %}
      {% if section.settings.title != blank or section.settings.description != blank %}
        <div class="content">
        {% if section.settings.title != blank %}<h2 class="banner-heading h1">{{ section.settings.title }}</h2>{% endif %}
        {% if section.settings.description != blank %}<div class="banner-desc rte"><p>{{ section.settings.description }}</p></div>{% endif %}
        {% if section.settings.btn_label != blank %}<a href="{{ section.settings.button_url }}" class="button btn-primary btn-primary-hover">{{ section.settings.btn_label }}</a>{% endif %}
      </div>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Designers Hero Banner",
  "tag": "section",
  "settings": [
    {
      "type": "image_picker",
      "label": "Desktop Image",
      "id": "desktop_image"
    },
    {
      "type": "image_picker",
      "label": "Mobile Image",
      "id": "mobile_image"
    },
    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "text",
      "label": "Title",
      "id": "title"
    },
    {
      "type": "text",
      "label": "Description",
      "id": "description"
    },
    {
      "type": "text",
      "label": "Button Label",
      "id": "btn_label"
    },
    {
      "type": "url",
      "label": "button url",
      "id": "button_url"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Designers Hero Banner"
    }
  ]
}
{% endschema %}